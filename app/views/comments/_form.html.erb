<% if user_signed_in? %>
  <%= form_for([@recipe.user, @recipe, @recipe.comments.build]) do |f| %>
    <%= f.label :content %><br>
    <%= f.text_area :content %><br>
    <br>
    <%= f.submit %>
  <% end %>
<% end %>